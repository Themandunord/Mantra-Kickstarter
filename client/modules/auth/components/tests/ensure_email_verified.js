const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import EnsureEmailVerified from '../ensure_email_verified';

describe('auth.components.ensure_email_verified', () => {
  it('should do something');
});
